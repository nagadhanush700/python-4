TASK ONE
Opens and reads a text file named sample.txt

Prints the contents of the file line by line

Handles errors gracefully if the file does not exist using exception handling



TASK TWO
Takes user input and writes it to a file named output.txt

Appends additional user input to the same file

Reads and displays the final content of the file

Displays appropriate success messages as per the expected output format
